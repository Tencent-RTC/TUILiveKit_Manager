/**
 * tuikit-atomicx-vue3 组件/枚举 - Vue 适配层
 *
 * 直接导入，由消费端的 alias 决定使用 Mock 还是真实实现。
 */
export { BarrageList, BarrageInput, LiveView, LiveAudienceList, LiveListEvent } from 'tuikit-atomicx-vue3/live';
